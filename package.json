{
  "name": "gd",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "mkdir -p ./backup && node ./server/server.js",
    "lint": "eslint ./server/server.js --ext .js",
    "lintFix": "eslint ./server/server.js --ext .js --fix"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "googleapis": "^14.2.0",
    "jszip": "^3.1.3",
    "node-zip": "^1.1.1",
    "rimraf": "^2.5.4"
  }
}
